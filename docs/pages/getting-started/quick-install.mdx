## Quick Install with `pip`:

Install R2R swiftly using `pip` to get started with minimal setup. This method will get you setup with the default configuration:

```bash
   # use the `'r2r[all]'` to download all required deps
   pip install 'r2r[parsing]'
```

## Setup the Environment Variables:

Copy the `.env.example` to `.env`

```bash
cp .env.example .env
```

Modify `config.json{:bash}` with your options of choice.

## Demo: Installation and launching Basic App

[![demo_screenshot](./demo_screenshot.png)](https://github.com/SciPhi-AI/R2R/assets/68796651/01fee645-1beb-4096-9e7d-7d0fa01386ea)
