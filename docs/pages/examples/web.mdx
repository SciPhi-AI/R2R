# Web Application

This example runs a nextjs web application which renders the live output from the RAG pipeline.

   ```bash
   cd $workdir/web && pnpm install
   
   # Serve the web app
   pnpm dev
   ```

![Web App](./events.png)
