# Build the docker container with the following command
docker build -t meltingrock/ask-api-clustering:latest -f Dcokerfile.clustering .

# Push to docker hub
docker push meltingrock/ask-api-clustering:latest